# user-management-system
This project is a simple User Management System built using Python. It allows users to register by providing their name, age, and email, with proper input validation and error handling.

The system stores user data in a text file and provides options to view registered users through a menu-driven interface.

Features include:
- User registration with validation
- Error handling for invalid inputs
- File-based data storage
- View all registered users
